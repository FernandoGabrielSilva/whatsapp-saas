{
  "name": "whatsapp-saas-single",
  "type": "module",
  "scripts": {
    "dev": "nodemon apps/api/src/index.js",
    "start": "node apps/api/src/index.js",
    "build": "cd apps/web && npm install && npm run build",
    "build:all": "npm install && cd apps/web && npm install && npm run build && npx prisma generate",
    "render:build": "npm ci && cd apps/web && npm ci && npm run build && cd ../.. && npx prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "@whiskeysockets/baileys": "6.5.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.3",
    "p-queue": "^7.4.1",
    "qrcode": "^1.5.3",
    "sharp": "^0.30.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "prisma": "^5.22.0"
  }
}
