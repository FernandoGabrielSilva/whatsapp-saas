{
  "name": "whatsapp-saas-single",
  "type": "module",
  "scripts": {
    "web:install": "cd apps/web && npm install",
    "web:build": "cd apps/web && npm run build",
    "start": "node apps/api/src/index.js",
    "build": "npm run web:install && npm run web:build && npx prisma generate"
  },
  "dependencies": {
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.3",
    "@prisma/client": "^5.22.0",
    "baileys": "^6.7.8",
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "p-queue": "^7.4.1",
    "qrcode": "^1.5.3"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}
